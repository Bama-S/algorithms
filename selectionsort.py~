def selection(a):
    for i in range(0,len(a)-1):
        indmin = i
        for j in range(i+1,len(a)):
            if a[j]<=a[indmin]:
                indmin = j
        if indmin != i:
            a[i],a[indmin] = a[indmin],a[i]
    print a

alist = [6,5,4,3,2,1]
selection(alist)